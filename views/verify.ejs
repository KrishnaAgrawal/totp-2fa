<!DOCTYPE html>
<html lang="en">

    <head>
        <title>OTP Verification</title>
        <script src="https://cdn.tailwindcss.com"></script>

        <!-- Confetti Library -->
        <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

        <script>
            function launchConfetti() {
                confetti({ particleCount: 900, spread: 90 });
            }
        </script>
    </head>

    <body class="bg-gray-100 dark:bg-black min-h-screen flex items-center justify-center fade-in">
        <div class="bg-white dark:bg-gray-800 text-center p-10 rounded-3xl shadow-xl max-w-md w-full">

            <% if (verified) { %>
                <script>setTimeout(launchConfetti, 300);</script>

                <div class="text-green-500 text-7xl mb-4">✔</div>
                <h1 class="text-3xl font-bold mb-4 dark:text-white">OTP Verified!</h1>
                <p class="text-gray-600 dark:text-gray-300">Your two-factor authentication is now active.</p>

                <% } else { %>
                    <div class="text-red-500 text-7xl mb-4">✘</div>
                    <h1 class="text-3xl font-bold mb-4 dark:text-white">Invalid OTP</h1>
                    <% } %>

                        <a href="/"
                            class="inline-block mt-8 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-xl shadow-lg transition">
                            Back
                        </a>

        </div>
    </body>
</html>